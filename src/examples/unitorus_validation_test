// Test invalid configuration - mismatched parameter counts
topology = unitorus;
dim_sizes = {3,4};           // 2 dimensions

// Intentionally wrong: 3 bandwidth values for 2 dimensions
dim_bandwidth = {1,2,3};     // Should cause error - 3 values for 2 dims
dim_latency = {1,1};
dim_penalty = {0,0};

routing_function = dim_order_unitorus;
num_vcs = 4;
vc_buf_size = 8;
vc_allocator = islip;
sw_allocator = islip;
traffic = uniform;
injection_rate = 0.01;
sim_type = latency;
sample_period = 100;
max_samples = 1;
