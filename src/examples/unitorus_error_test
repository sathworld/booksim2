// Test configuration with INTENTIONAL ERRORS to test validation
// This should fail with helpful error messages

// Topology
topology = unitorus;
dim_sizes = "4,6";        // 2 dimensions

// Direction-specific configuration - WRONG: 3 values for 2 dimensions
dim_bandwidth = "2,1,3";     // ERROR: 3 values for 2 dimensions
dim_latency = "1,2";         // CORRECT: 2 values for 2 dimensions
dim_penalty = "0";           // ERROR: 1 value for 2 dimensions

// Routing
routing_function = dim_order_unitorus;

// Flow control
num_vcs = 4;
vc_buf_size = 8;

// Traffic
traffic = uniform;
injection_rate = 0.1;

// Simulation
sim_type = latency;
warmup_periods = 1;
sample_period = 100;
max_samples = 1;
