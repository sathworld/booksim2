// Test penalty-aware routing with equal penalties (should behave like original)

topology = unitorus;
dim_sizes = {4,4};         // 4x4 torus

// Equal penalties - should behave like original dimension-ordered
dim_bandwidth = {1,1};
dim_latency = {1,1};
dim_penalty = {0.0,0.0};   // Equal penalties

routing_function = dim_order_unitorus;

// Flow control
num_vcs = 8;
vc_buf_size = 16;

// Router architecture  
vc_allocator = islip;
sw_allocator = islip;

// Traffic
traffic = uniform;
injection_rate = 0.02;

// Simulation
sim_type = latency;
sample_period = 500;
max_samples = 3;
