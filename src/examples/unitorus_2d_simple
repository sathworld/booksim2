// Simple 2D UniTorus test to debug segfault
topology = unitorus;
dim_sizes = {2,2};         // 2x2 torus

// Simple configuration
dim_bandwidth = {1,1};     
dim_latency = {1,1};       
dim_penalty = {0,0};       

// Routing
routing_function = dim_order_unitorus;

// Minimal flow control
num_vcs = 2;
vc_buf_size = 4;

// Simple traffic
traffic = uniform;
injection_rate = 0.01;

// Quick simulation
sim_type = latency;
sample_period = 100;
max_samples = 1;
